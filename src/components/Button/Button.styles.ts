import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    flex: 1,
    height: 45,
    backgroundColor: '#333',
    borderRadius: 4,
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    color: '#fff',
  },
});
