import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  root: {
    padding: 20,
  },
  boxesRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  button: {
    marginTop: 25,
  },
});
